<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.loginactivity">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Astigmatic_Helper"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="Astigmatic.ShoppingTabActivity"
            android:label="@string/title_activity_shopping_tab"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".TransparentActivity"
            android:theme="@style/Theme.Transparent" />
        <activity android:name="Astigmatic.GPSAccessActivity" />
        <activity android:name="Astigmatic.ReadingActivity" />
        <activity android:name="Astigmatic.GPSActivity" />
        <activity android:name="Astigmatic.ShoppingActivity" />
        <activity android:name="Astigmatic.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>